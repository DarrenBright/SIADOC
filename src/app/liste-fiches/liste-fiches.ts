import { Component } from '@angular/core';

@Component({
  selector: 'app-liste-fiches',
  standalone: true,
  templateUrl: './liste-fiches.html',
  styleUrls: ['./liste-fiches.scss']
})
export class ListeFiches {
}

